{
  "name": "eigenlayer-mcp-server",
  "version": "1.0.0",
  "description": "A Model Context Protocol (MCP) server for EigenLayer documentation, designed to run on Vercel.",
  "main": "index.js",
  "scripts": {
    "build": "tsc && mkdir -p build/static && cp -r static/* build/static/ && chmod 755 build/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bin": {
    "eigenlayer-mcp-server": "./build/index.js"
  },
  "files": [
    "build"
  ],
  "devDependencies": {
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.9.0"
  }
}
